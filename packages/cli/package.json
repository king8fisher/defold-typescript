{
  "name": "@defold-ts/cli",
  "version": "0.0.0",
  "description": "End-user CLI for scaffolding and building Defold projects written in TypeScript.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "bin": {
    "defold-ts": "./src/bin.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@defold-ts/transpiler": "workspace:*",
    "@defold-ts/types": "workspace:*"
  }
}
